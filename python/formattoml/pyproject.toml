[project]
name = "formattoml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    'utils',
]

[tool.uv.sources]
utils = { workspace = true }
