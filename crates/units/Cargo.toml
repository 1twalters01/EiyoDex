[package]
name = "units"
version = "0.1.0"
edition = "2021"

[dependencies]
mass_macro = { path = "../proc_macros/mass_macro" }
currency_macro = { path = "../proc_macros/currency_macro" }
serde = { version = "1.0", features = ["derive"], optional = true }
redis = { version = "0.25.2", optional = true }
reqwest = { version = "0.12.4", features = ["json"] }
chrono = "0.4.42"

[features]
default = []
serde = [
    'dep:serde',
]
redis = [
    'dep:redis',
]
