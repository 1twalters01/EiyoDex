[package]
name = "foods"
version = "0.1.0"
edition = "2021"

[dependencies]
nutrients = { path = "../nutrients" }
uuid = { version = "1.8.0", features = ["v4", "fast-rng", "macro-diagnostics", "serde"] }
