[package]
name = "profiles"
version = "0.1.0"
edition = "2021"

[dependencies]
utils = { path = "../utils" }
units = { path = "../units" }
chrono = "0.4.42"
uuid = { version = "1.8.0", features = ["v4", "fast-rng", "macro-diagnostics", "serde"] }

