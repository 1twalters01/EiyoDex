[package]
name = "exercise"
version = "0.1.0"
edition = "2021"

[dependencies]
identity = { path = "../../../core/identity" }
uuid = { version = "1.8.0", features = ["v4", "fast-rng", "macro-diagnostics", "serde"] }
units = { path = "../../../core/units" }
